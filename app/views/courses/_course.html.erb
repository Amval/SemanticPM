<div class="item" id="course-<%= course.id %>">
  <div class="content">
    <a class="header">
      <%= course.name %>
      <% if !course.concept.nil? %>
        <div class="ui label">
          <%= course.prueba %>
        </div>
      <% end %>
    </a>
  </div>
</div>